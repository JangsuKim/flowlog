spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowlog?serverTimezone=Asia/Tokyo&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: JsDev@2025
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mvc:
    cors:
      mappings:
        - path-pattern: "/**"
          allowed-origins: "http://localhost:5173"
          allowed-methods: "*"
          allowed-headers: "*"
          allow-credentials: true

server:
  port: 8080